## SistemaConnect (ISP Manager)

Monorepo con:

- **backend**: API en Python (Flask) + MySQL + JWT + módulos (Clientes, Facturación, Mikrotik, AFIP).
- **frontend**: React (cliente web) consumiendo la API.

### Estructura (alta nivel)

```
SistemaConnect/
  backend/
  frontend/
```

